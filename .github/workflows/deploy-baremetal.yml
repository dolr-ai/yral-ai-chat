name: Setup yral-ai-chat Deploy User

on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  prepare-servers:
    name: Prepare Server List
    runs-on: ubuntu-latest
    outputs:
      servers: ${{ steps.parse.outputs.servers }}
    steps:
      - name: Parse server IPs
        id: parse
        run: |
          ALL_IPS=$(echo "${{ vars.BAREMETAL_SERVER_IPS }},${{ vars.PG_SERVER_IP }}" | tr ',' '\n' | sed '/^$/d' | tr '\n' ',' | sed 's/,$//')
          JSON=$(echo "$ALL_IPS" | tr ',' '\n' | jq -R . | jq -s -c .)
          echo "servers=$JSON" >> $GITHUB_OUTPUT

  generate-and-store-key:
    name: Generate SSH Key and Store in Vault
    runs-on: ubuntu-latest

    outputs:
      public_key: ${{ steps.keygen.outputs.public_key }}

    steps:
      - name: Generate SSH key pair
        id: keygen
        run: |
          ssh-keygen -t ed25519 \
            -C "yral-ai-chat-deploy-$(date +%Y%m%d)" \
            -f /tmp/new_deploy_key \
            -N ""
          echo "public_key=$(cat /tmp/new_deploy_key.pub)" >> $GITHUB_OUTPUT

      - name: Install Vault CLI
        run: |
          wget -O- https://apt.releases.hashicorp.com/gpg | sudo gpg --dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gpg
          echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list
          sudo apt-get update && sudo apt-get install -y vault

      - name: Authenticate to Vault
        uses: hashicorp/vault-action@v3
        with:
          url: https://vault.yral.com
          method: jwt
          role: yral-ai-chat-write-role
          jwtGithubAudience: "https://github.com/dolr-ai"
          exportToken: true

      - name: Store private key in Vault
        run: |
          NEW_PRIVATE_KEY=$(cat /tmp/new_deploy_key)
          echo "::add-mask::$NEW_PRIVATE_KEY"

          vault kv put secret/yral-ai-chat/SSH_PRIVATE_KEY value="$NEW_PRIVATE_KEY"
          vault kv put secret/yral-ai-chat/PG_SSH_PRIVATE_KEY value="$NEW_PRIVATE_KEY"

          echo "Private key stored in Vault at secret/yral-ai-chat/SSH_PRIVATE_KEY and secret/yral-ai-chat/PG_SSH_PRIVATE_KEY"
        env:
          VAULT_ADDR: https://vault.yral.com

      - name: Cleanup
        if: always()
        run: |
          rm -f /tmp/new_deploy_key /tmp/new_deploy_key.pub

  setup-user:
    name: Setup yral-ai-chat on ${{ matrix.server }}
    needs: [prepare-servers, generate-and-store-key]
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        server: ${{ fromJson(needs.prepare-servers.outputs.servers) }}

    steps:
      - name: Setup root SSH key
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.HETZNER_BARE_METAL_GITHUB_ACTIONS_SSH_PRIVATE_KEY }}

      - name: Add server to known hosts
        run: |
          ssh-keyscan -H ${{ matrix.server }} >> ~/.ssh/known_hosts

      - name: Verify root connectivity
        run: |
          ssh -o ConnectTimeout=10 root@${{ matrix.server }} \
            "echo 'Root connection successful on ${{ matrix.server }}'"

      - name: Create user and setup SSH on server
        run: |
          PUB_KEY="${{ needs.generate-and-store-key.outputs.public_key }}"

          ssh root@${{ matrix.server }} "
            set -e

            # Create user if not exists
            if id 'yral-ai-chat' &>/dev/null; then
              echo 'User yral-ai-chat already exists, skipping creation'
            else
              useradd -m -s /bin/bash 'yral-ai-chat'
              echo 'User yral-ai-chat created'
            fi

            # Setup .ssh directory
            mkdir -p /home/yral-ai-chat/.ssh
            chmod 700 /home/yral-ai-chat/.ssh

            # Add public key
            echo '$PUB_KEY' > /home/yral-ai-chat/.ssh/authorized_keys
            chmod 600 /home/yral-ai-chat/.ssh/authorized_keys
            chown -R yral-ai-chat:yral-ai-chat /home/yral-ai-chat/.ssh

            # Add to docker group
            usermod -aG docker yral-ai-chat
            echo 'User yral-ai-chat added to docker group'

            echo 'Setup complete on ${{ matrix.server }}'
          "

      - name: Import SSH key from Vault to verify
        uses: hashicorp/vault-action@v3
        with:
          url: https://vault.yral.com
          method: jwt
          role: yral-ai-chat-role
          jwtGithubAudience: "https://github.com/dolr-ai"
          secrets: |
            secret/data/yral-ai-chat/SSH_PRIVATE_KEY value | SSH_PRIVATE_KEY ;

      - name: Verify SSH access with new key
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ env.SSH_PRIVATE_KEY }}

      - name: Test SSH and docker group
        run: |
          ssh -o PasswordAuthentication=no \
              yral-ai-chat@${{ matrix.server }} \
              "echo 'SSH verified on ${{ matrix.server }}' && groups | grep docker && echo 'Docker group verified' || echo 'WARNING: docker group not active yet'"