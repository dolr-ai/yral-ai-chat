name: Rotate SSH Keys
 
on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read
 
jobs:
  rotate-ssh-keys:
    name: Rotate SSH Keys
    runs-on: ubuntu-latest
 
    steps:
      - name: Import Secrets from Vault
        uses: hashicorp/vault-action@v3
        with:
          url: https://vault.yral.com
          method: jwt
          role: yral-ai-chat-role
          jwtGithubAudience: "https://github.com/dolr-ai"
          secrets: |
            secret/data/yral-ai-chat/SSH_PRIVATE_KEY value | SSH_PRIVATE_KEY ;
 
      - name: Setup current SSH key
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ env.SSH_PRIVATE_KEY }}
 
      - name: Add servers to known hosts
        run: |
          IPS="${{ vars.BAREMETAL_SERVER_IPS }}"
          for ip in $(echo "$IPS" | tr ',' ' '); do
            ssh-keyscan -H "$ip" >> ~/.ssh/known_hosts
          done
 
      - name: Generate new SSH key pair
        run: |
          ssh-keygen -t ed25519 -C "yral-ai-chat-deploy-$(date +%Y%m%d)" -f /tmp/new_deploy_key -N ""
          echo "NEW_PUBLIC_KEY=$(cat /tmp/new_deploy_key.pub)" >> $GITHUB_ENV
 
      - name: Rotate public key on all servers
        run: |
          IPS="${{ vars.BAREMETAL_SERVER_IPS }}"
          NEW_PUB_KEY="${{ env.NEW_PUBLIC_KEY }}"
          for ip in $(echo "$IPS" | tr ',' ' '); do
            echo "Rotating key on $ip..."
            ssh yral-ai-chat@$ip "
              set -e
 
              # Append new key first (so we don't lock ourselves out)
              echo '$NEW_PUB_KEY' >> ~/.ssh/authorized_keys
 
              echo 'New key added to $ip'
            "
          done
 
      - name: Authenticate to Vault
        uses: hashicorp/vault-action@v3
        with:
          url: https://vault.yral.com
          method: jwt
          role: yral-ai-chat-write-role
          jwtGithubAudience: "https://github.com/dolr-ai"
          exportToken: true
 
      - name: Install Vault CLI
        run: |
          wget -O- https://apt.releases.hashicorp.com/gpg | sudo gpg --dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gpg
          echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list
          sudo apt-get update && sudo apt-get install -y vault
 
      - name: Update private key in Vault
        run: |
          NEW_PRIVATE_KEY=$(cat /tmp/new_deploy_key)
 
          vault kv put secret/yral-ai-chat/SSH_PRIVATE_KEY value="$NEW_PRIVATE_KEY"
        env:
          VAULT_ADDR: https://vault.yral.com
 
      - name: Verify new key works on all servers
        run: |
          # Load the new private key
          ssh-add -D  # Remove old keys from agent
          ssh-add /tmp/new_deploy_key
 
          IPS="${{ vars.BAREMETAL_SERVER_IPS }}"
          for ip in $(echo "$IPS" | tr ',' ' '); do
            echo "Verifying new key on $ip..."
            ssh -o PasswordAuthentication=no \
                yral-ai-chat@$ip \
                "echo 'New key verified on $ip'"
          done
 
      - name: Remove old public key from all servers
        run: |
          IPS="${{ vars.BAREMETAL_SERVER_IPS }}"
          OLD_PUB_KEY=$(ssh-add -L | head -1)
          NEW_PUB_KEY="${{ env.NEW_PUBLIC_KEY }}"
 
          for ip in $(echo "$IPS" | tr ',' ' '); do
            echo "Removing old key from $ip..."
            ssh yral-ai-chat@$ip "
              set -e
              # Keep only lines matching the new public key
              grep -F '$NEW_PUB_KEY' ~/.ssh/authorized_keys > /tmp/authorized_keys_new
              mv /tmp/authorized_keys_new ~/.ssh/authorized_keys
              chmod 600 ~/.ssh/authorized_keys
              echo 'Old key removed from $ip'
            "
          done
 
      - name: Cleanup key material
        if: always()
        run: |
          rm -f /tmp/new_deploy_key /tmp/new_deploy_key.pub
          ssh-add -D || true
 